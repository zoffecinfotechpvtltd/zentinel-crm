NODE_ENV=development
PORT=4000

DATABASE_URL=postgres://zoffec:zoffec_dev_local@localhost:5432/zoffec_cms

SESSION_COOKIE_NAME=zoffec_sid
SESSION_TTL_HOURS=12
SESSION_REMEMBER_TTL_DAYS=30

# used for password reset token links
APP_BASE_URL=http://localhost:5173

# SMTP (Brevo free tier: 300 emails/day, no card needed — sign up at brevo.com,
# then Settings > SMTP & API > SMTP tab for these values).
# Leave blank to fall back to console-logging emails in dev.
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=Zoffec CMS <no-reply@zoffec.com>
